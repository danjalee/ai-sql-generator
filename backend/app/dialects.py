DIALECT_RULES = {
    "mysql": "Use MySQL syntax. Use LIMIT for pagination.",
    "postgresql": "Use PostgreSQL syntax. Use LIMIT and OFFSET.",
    "sqlserver": "Use SQL Server syntax. Use TOP instead of LIMIT.",
    "sqlite": "Use SQLite syntax. Use LIMIT."
}