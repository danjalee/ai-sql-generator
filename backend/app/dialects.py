DIALECT_RULES = {
    "mysql": "Use MySQL syntax. LIMIT allowed.",
    "postgresql": "Use PostgreSQL syntax.",
    "sqlserver": "Use SQL Server syntax. Use TOP instead of LIMIT.",
    "sqlite": "Use SQLite syntax."
}
