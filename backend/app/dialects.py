DIALECT_RULES = {
    "mysql": "Use MySQL syntax. Use LIMIT. Support WITH.",
    "postgresql": "Use PostgreSQL syntax. Support WITH.",
    "sqlserver": "Use SQL Server syntax. Use TOP instead of LIMIT.",
    "sqlite": "Use SQLite syntax. Support WITH."
}
